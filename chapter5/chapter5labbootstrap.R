# bootstrap
# minimum risk investment section 5.2

alpha = function(x,y) {
  vx = var(x)
  vy = var(y)
  cxy = cov(x,y)
  # (vy - cxy)/(vx + vy - 2*cxy)
}

alpha(Portfolio$X, Portfolio$Y)
Portfolio
class(Portfolio$X)
